{
    "name": "client",
    "version": "1.0.0",
    "main": "client.ts",
    "license": "MIT",
    "scripts": {
        "build": "tsc",
        "start:client": "yarn run build && node dist/client.js",
        "start:server": "yarn run build && node dist/server.js",
        "clean": "rm -rf dist"
    },
    "bin": {},
    "dependencies": {
        "@types/express-ws": "^3.0.0",
        "@types/node": "^13.11.1",
        "express": "^4.17.1",
        "express-ws": "^4.0.0",
        "typescript": "^4.1.5"
    },
    "devDependencies": {}
}